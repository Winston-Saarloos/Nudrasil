services:
  nudrasil-app:
    build:
      context: ../../
      dockerfile: docker/production/Dockerfile
    container_name: nudrasil-app-production
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
